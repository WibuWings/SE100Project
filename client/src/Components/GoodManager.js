import React, { Component } from 'react';

class GoodManager extends Component {
    render() {
        return (
            <div>
                GoodManager
            </div>
        );
    }
}

export default GoodManager;